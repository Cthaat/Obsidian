---
topic: 量化部署
parent: 系统架构
tags: [event-driven, architecture, backtesting, live-trading]
created: 2026-06-02
---

# 事件驱动架构详解

> [!note] 概述
> 事件驱动架构（Event-Driven Architecture）是量化交易系统的核心设计模式，使回测代码能无缝迁移到实盘环境。

## 什么是事件驱动？

在事件驱动系统中，所有操作都由**事件**触发，按时间顺序逐一处理：

```
时间线：09:30:00 → 09:30:01 → 09:30:02 → ...

事件流：
  MarketEvent (新K线/新Tick)
      ↓
  SignalEvent (策略产生信号)
      ↓
  OrderEvent (订单发送)
      ↓
  FillEvent (成交回报)
```

## 核心组件

### 1. 事件队列（Event Queue）

```python
from collections import deque

class EventQueue:
    """FIFO事件队列"""
    def __init__(self):
        self.queue = deque()

    def push(self, event):
        self.queue.append(event)

    def pop(self):
        return self.queue.popleft() if self.queue else None
```

### 2. 数据处理器（DataHandler）

```python
class DataHandler:
    """提供历史和实时数据"""
    def get_latest_bars(self, symbol, N=1):
        """获取最近N根K线"""
        pass

    def update_bars(self):
        """产生MarketEvent，推进时间"""
        pass
```

### 3. 策略（Strategy）

```python
class Strategy:
    """接收MarketEvent → 计算信号 → 产生SignalEvent"""
    def on_bar(self, event):
        # 每个新bar执行
        pass
```

### 4. 组合管理（Portfolio）

```python
class Portfolio:
    """追踪仓位、现金、PnL"""
    def update_fill(self, fill_event):
        pass

    def update_timeindex(self, event):
        pass
```

### 5. 执行处理器（ExecutionHandler）

```python
class ExecutionHandler:
    """将OrderEvent发送到券商/交易所"""
    def execute_order(self, order_event):
        # 回测模式：模拟撮合
        # 实盘模式：真实下单
        pass
```

## 回测 vs 实盘统一架构

```
        回测                        实盘
        ────                        ────
DataHandler(读CSV/DB)  ←→  DataHandler(读WebSocket/API)
Portfolio(模拟撮合)     ←→  Portfolio(真实成交)
Execution(模拟)         ←→  Execution(券商API)
```

**同源代码收益**：回测和实盘使用相同的Strategy、Portfolio、RiskManager代码，只替换DataHandler和ExecutionHandler的实现。

## 事件类型设计

```python
class EventType(Enum):
    MARKET = 'MARKET'         # 行情更新
    SIGNAL = 'SIGNAL'         # 交易信号
    ORDER = 'ORDER'           # 订单
    FILL = 'FILL'             # 成交
    RISK = 'RISK'             # 风控事件
    SYSTEM = 'SYSTEM'         # 系统事件（启动/关闭）
```

## Homalos 事件驱动系统

[Homalos](https://homalos.github.io/) 是一个开源的事件驱动量化系统：

- 数据中心：统一管理多数据源
- 事件总线：组件间异步解耦通信
- 策略容器：热加载/热更新策略
- 实时风控：规则引擎 + 动态阈值

## 设计要点

1. **单一职责**：每个组件只做一件事
2. **松耦合**：组件通过事件通信，不直接依赖
3. **可替换性**：DataHandler/ExecutionHandler可插拔
4. **时间一致性**：回测和实盘遵循相同的时间语义
5. **状态管理**：系统状态可序列化、可恢复

## 开源参考

- [QuantBacktest](https://github.com/Orange1021/QuantBacktest) (GitHub): 事件驱动回测框架完整实现
- [Homalos](https://github.com/Homalos/Homalos): 事件驱动多资产量化系统
- Zipline: Quantopian的事件驱动回测引擎
