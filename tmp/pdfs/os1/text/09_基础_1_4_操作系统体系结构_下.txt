
===== 基础_1.4 操作系统体系结构（下） | PDF p1 =====
本节内容
操作系统的
体系结构
王道考研/CSKAOYAN.COM
[OCR]
本节内容
操作系统的
体系结构
王道考研/CSKAOYAN.COM

===== 基础_1.4 操作系统体系结构（下） | PDF p2 =====
王道考研/CSKAOYAN.COM
知识总览
Tips：一定是考简单的选择题，了解各种体系结构的特性，了解各自的优缺点
[OCR]
知识总览
大内核 (又名：宏内核/单内核）
微内核
操作系统体系结构
分层结构
模块化
对新知识充满期待
外核
Tips：一定是考简单的选择题，了解各种体系结构的特性，了解各自的优缺点
王道考研/CSKAOYAN.COM

===== 基础_1.4 操作系统体系结构（下） | PDF p3 =====
王道考研/CSKAOYAN.COM
操作系统结构——总结
[OCR]
操作系统结构
特性、思想
优点
缺点
1.便于调试和验证，自底向上逐层调试验证
1.仅可调用相邻低层，难以合理定义各层的边界
分层结构
内核分多层，每层可单向调用更低一层提供的接
口
2.易扩充和易维护，各层之间调用接口清晰固定
2.效率低，不可跨层调用，系统调用执行时间长
1.模块间逻辑清晰易于维护，确定模块间接口后
将内核划分为多个模块，各模块之间相互协作。
即可多模块同时开发
1.模块间的接口定义未必合理、实用
内核=主模块+可加载内核模块
2.支持动态加载新的内核模块（如：安装设备驱
模块化
主模块：只负责核心功能，如进程调度、内存管
动程序、安装新的文件系统模块到内核），增强
理
OS适应性
可加载内核模块：可以动态加载新模块到内核，
而无需重新编译整个内核
2.模块间相互依赖，更难调试和验证
3.任何模块都可以直接调用其他模块，无需采用
消息传递进行通信，效率高
1.内核庞大功能复杂，难以维护
所有的系统功能都放在内核里（大内核结构的OS
1.性能高，内核内部各种功能都可以直接相互调
宏内核 (大内核)
通常也采用了"模块化"的设计思想)
用
2.大内核中某个功能模块出错，就可能导致整个
系统崩溃
1.性能低，需要频繁的切换用户态/核心态。用
1.内核小功能少、易于维护，内核可靠性高
户态下的各功能模块不可以直接相互调用，只能
只把中断、原语、进程通信等最核心的功能放入
通过内核的"消息传递"来间接通信
微内核
内核。进程管理、文件管理、设备管理等功能以
用户进程的形式运行在用户态
2.内核外的某个功能模块出错不会导致整个系统
2.用户态下的各功能模块不可以直接相互调用，
崩溃
只能通过内核的"消息传递"来间接通信
1.外核可直接给用户进程分配"不虚拟、不抽象"
1.降低了系统的一致性
内核负责进程调度、进程通信等功能，外核负责
的硬件资源，使用户进程可以更灵活的使用硬件
外核 (exokernel)
为用户进程分配未经抽象的硬件资源，且由外核
资源
负责保证资源使用安全
2.使系统变得更复杂
2.减少了虚拟硬件资源的"映射层"，提升效率
王道考研/CSKAOYAN.COM

===== 基础_1.4 操作系统体系结构（下） | PDF p4 =====
王道考研/CSKAOYAN.COM
操作系统结构——分层结构
最底层是硬件，最高层是用户接口
每层可调用更低一层
[OCR]
操作系统结构一一分层结构
层N
用户接口
层1
最底层是硬件，最高层是用户接口
层0
硬件
每层可调用更低一层
王道考研/CSKAOYAN.COM

===== 基础_1.4 操作系统体系结构（下） | PDF p5 =====
王道考研/CSKAOYAN.COM
操作系统结构——模块化
[OCR]
操作系统结构一一模块化
（2）模块化
模块化是将操作系统按功能划分为若干个具有一定独立性的模块。每个模块具有某方面的管
理功能，并规定好各模块间的接口，使各模块之间能通过接口进行通信。还可以进一步将各模块
细分为若干个具有一定功能的子模块，同样也规定好各子模块之间的接口。把这种设计方法称为
模块-接口法，图1.3所示为由模块、子模块等组成的模块化操作系统结构。
操作系统
模块
进程管理
存储器管理
文件管理
子模块
进程控制
进程调度
内存分配
内存保护
磁盘管理
目录管理
图1.3
模块化结构的操作系统
王道考研/CSKAOYAN.COM

===== 基础_1.4 操作系统体系结构（下） | PDF p6 =====
王道考研/CSKAOYAN.COM
操作系统结构——宏内核、微内核
进程
管理
存储
管理
设备
管理
时钟管理、中断处理、
原语
应用程序
进程
管理
存储
管理
设备
管理
时钟管理、中断处理、
原语、进程通信、调度
应用程序
内核态
用户态
大内核
微内核
内核态
用户态

===== 基础_1.4 操作系统体系结构（下） | PDF p7 =====
王道考研/CSKAOYAN.COM
操作系统结构——外核（exokernel）
[OCR]
操作系统结构－一外核（exokernel)
Hardware
NormalKernel
Exokernel
Programs communicate with Libraries or Kernel
Programs can
communicate with the
Kernel
hardware much more
directly
Library
Library
Programs
王道考研/CSKAOYAN.COM

===== 基础_1.4 操作系统体系结构（下） | PDF p8 =====
[OCR]
公众号：王道在线
b站：王道计算机教育
抖音：王道计算机考研
